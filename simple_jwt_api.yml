apiVersion: v1
kind: Service
metadata:
  name: simple-jwt-api
spec:
  ports:
  - nodePort: 30500
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: simple-jwt-api
  type: LoadBalancer